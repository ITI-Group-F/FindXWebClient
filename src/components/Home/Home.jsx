import React from 'react';
import ActionAreaCard from '../Items/allItems';
import Items from '../Items/items';

function Home(props) {
    return (
        <div>
           <ActionAreaCard/>
        </div>
    );
}

export default Home;