/* import { actionTypes } from "../Actions/authenticationActionsAndActionTypes";

const initialState= null;
 
 
 const authenticationReducer = (state=initialState,{type,payload})=>{
     
      switch (type) {
       case :
           
           break;
   
       default:
           break;
   } 
 }
 
 export default authenticationReducer; */